<h4>Notifications</h4>
<p><%= discussion.subscribed_reason(user)%></p>

<% if discussion.subscribed?(user) %>
  <%= button_to "Unsubscribe", discussion_notifications_path(discussion), class: "btn btn-danger btn-sm" %>
<% else %>
  <%= button_to "Subscribe", discussion_notifications_path(discussion), class: "btn btn-success btn-sm" %>
<% end %>